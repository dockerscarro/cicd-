name: CI/CD Pipeline (QA)

on:
  push:
    branches:
      - qa

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type check
        run: npx tsc --noEmit

  docker-test:
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: docker build -t dgm-remapp:test .

      - name: Run container for smoke test
        run: |
          docker run -d --name dgm-remapp-test -p 3002:3000 dgm-remapp:test
          sleep 10
          if [ "$(docker inspect -f '{{.State.Running}}' dgm-remapp-test)" != "true" ]; then
            echo "❌ Container crashed or exited"
            docker logs dgm-remapp-test
            exit 1
          fi
          echo "✅ Container is running"

      - name: HTTP smoke test
        run: curl -f http://localhost:3002

      - name: Print container logs (debug)
        if: always()
        run: docker logs dgm-remapp-test

      - name: Cleanup
        if: always()
        run: |
          docker stop dgm-remapp-test
          docker rm dgm-remapp-test

  # deploy-to-server:
  #   needs: docker-test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Setup SSH
  #       uses: webfactory/ssh-agent@v0.8.0
  #       with:
  #         ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}

  #     - name: Test SSH connection
  #       run: |
  #         ssh -o StrictHostKeyChecking=no -p ${{ secrets.SERVER_PORT }} ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} "echo '✅ Connected to server successfully!'"

  #     - name: Build and run Docker container on server
  #       run: |
  #         ssh -o StrictHostKeyChecking=no -p ${{ secrets.SERVER_PORT }} ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} <<'EOF'
  #         set -e
      
  #         APP_DIR=~/dgm-remapp
  #         CONTAINER_NAME=dgm-remapp
  #         REPO_URL=https://github.com/dockerscarro/cicd-.git
  #         BRANCH=qa
  #         PAT_TOKEN=${{ secrets.PAT_TOKEN }}
      
  #         # Install Docker if missing
  #         if ! command -v docker &> /dev/null; then
  #             sudo apt update
  #             sudo apt install -y docker.io git
  #             sudo systemctl enable --now docker
  #             sudo usermod -aG docker $USER
  #         fi
      
  #         # Clone repo first time, otherwise update
  #         if [ ! -d "$APP_DIR" ]; then
  #             git clone -b $BRANCH https://$PAT_TOKEN@github.com/dockerscarro/cicd-.git $APP_DIR
  #         else
  #             cd $APP_DIR
  #             git fetch --all
  #             git reset --hard origin/$BRANCH
  #         fi
      
  #         cd $APP_DIR
      
  #         # Build Docker image
  #         docker build -t $CONTAINER_NAME:qa .
      
  #         # Stop and remove existing container if exists
  #         if [ "$(docker ps -aq -f name=$CONTAINER_NAME)" ]; then
  #             docker stop $CONTAINER_NAME
  #             docker rm $CONTAINER_NAME
  #         fi
      
  #         # Run container
  #         docker run -d -p 3002:3000 --name $CONTAINER_NAME $CONTAINER_NAME:qa
      
  #         # Verify container is running
  #         sleep 5
  #         if [ "$(docker inspect -f '{{.State.Running}}' $CONTAINER_NAME)" != "true" ]; then
  #             docker logs $CONTAINER_NAME
  #             exit 1
  #         fi
      
  #         echo "✅ Container is running successfully on the server!"
  #         EOF

